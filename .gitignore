# Byte-compiled / cache files
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.cache/

# Logs
logs/

# OS-generated files
.DS_Store

# Native grids: regenerable via `python -m extract_um_met make-native-grid`.
# Keep the folder tracked (via .gitkeep) but not its contents.
data/*
!data/.gitkeep

# Personal config — copy config.example.yaml to config.yaml and edit locally.
config.yaml

# Sample/raw .pp data (large; never commit)
pp_data/
*.pp
*.pp.gz

# Sphinx docs build output
docs/_build/
